Devpost gallery images, in upload order. Two are real screen captures (Playwright), two are original explainer graphics built for the gallery (not slides pulled from the submission video), one is a real screenshot of the device itself.

1. `architecture.png` — original graphic, not in the video.
   Caption: "One board, two engines: the GPU simulates the flood, four Arm cores route and narrate, concurrently, on an $80 Pi 5."

2. `panel_idle.png` — real screen capture, the 800x480 kiosk panel at rest.
   Caption: "The panel on boot: a real self-test (sim, routing, narrator), not a static splash screen."

3. `console_storm.png` — real screen capture, desktop console mid-storm over Red Hook.
   Caption: "Hazard bands (H1-H4+) draped over live OpenStreetMap/Protomaps cartography, entirely offline."

4. `console_routes.png` — real screen capture, a tapped location with a stranded wheelchair route.
   Caption: "Tap a location; BFS routes by mobility profile against the same flood field. Here, no wheelchair-safe path exists, the panel says so instead of guessing."

5. `thresholds_corrected.png` — original graphic, not in the video.
   Caption: "The mobility-safety depths I originally guessed were wrong (off by 6x on the vehicle threshold). The shipped system uses the published standards instead."
